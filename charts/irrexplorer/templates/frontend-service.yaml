apiVersion: v1
kind: Service
metadata:
  name: {{ include "irrexplorer.frontendServiceName" . }}
  labels:
    {{- include "irrexplorer.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "irrexplorer.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
